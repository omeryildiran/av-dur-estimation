The new default resolves correctly — it reads the 11 boxcar lognorm fits, and the σ_a/σ_v group stats (0.315/0.169, 0.533/0.291) match exactly what I hardcoded into the grid runner's 'd' regime. Everything is consistent and self-checking.

  Everything is now wired for boxcar-consistent recovery

  run_model_recovery.py (empirical group recovery — feeds all three figures)
  - New --model_fits_dir arg, default model_fits/boxcarFits → the empirical parameter regime is now sampled from your boxcar fits (with an OMER — CHOOSE HERE comment to flip back to model_fits).
  - --save_dir default → model_recovery_results_group_boxcar_ns1_nsim2000 (commented; keeps old results intact). 

  run_model_recovery_grid.py (synthetic σ-grid cells)
  - 'd' empirical regime refreshed from the boxcar lognorm fits: _SIGMA_D_NORMAL = σ_a(0.315, 0.169), σ_v(0.533, 0.291); range σa 0.12–0.66 / σv 0.26–1.13. Old values preserved in the comment.
  - --save_dir default → model_recovery_grid_results_boxcar_ns1_nsim2000.

  grid_recovery_summary.ipynb (renders the figures)
  - GRID_DIR, EMP_DIR (both cells), PARAM_RECOV_EMP_DIR → boxcar dir names, each with an OMER — CHOOSE HERE comment.

  Corrected HPC plan — only two runs (not three)

  I was wrong earlier about a separate empirical_param_recovery_3model run: the param-recovery figure reads the same group dir as the confusion figures. So:

  # 1. Empirical group recovery -> empirical_confusion_3model + grid empirical panel + emprical_parameter_recovery_nIter_50
  python run_model_recovery.py --models lognorm fusionOnlyLogNorm switchingFree \
      --n_recovery 50 --nSimul 2000 --nStarts 1 --n_jobs 32
      # (boxcar defaults: --model_fits_dir model_fits/boxcarFits, --save_dir ..._group_boxcar_ns1_nsim2000)

  # 2. Synthetic σ-grid recovery -> grid_three_model_confusion_matrices (synthetic cells)
  python run_model_recovery_grid.py --models lognorm fusionOnlyLogNorm switchingFree \
      --nSimul 10000 --nStarts 1 --n_jobs 32

     python run_model_recovery_grid.py --models lognorm fusionOnlyLogNorm switchingFree \
      --n_iter 50 --nSimul 10000 --nStarts 1 --n_jobs 32   
      # (boxcar default: --save_dir ..._grid_results_boxcar_ns1_nsim10000)

    Then re-run grid_recovery_summary.ipynb locally to regenerate the three PDFs.

    Two notes:
    - The runner defaults now write to new _boxcar dirs, so your existing non-boxcar results are untouched — good for side-by-side sanity checks. The notebook points at the same _boxcar names, so no path juggling.
    - run_model_recovery_grid.py's default --models is the full 5-model set; I passed the 3 you need explicitly. If you also want the 5-model confusion (empirical_confusion_5model) on boxcar, note that boxcarFits only contains the 3 models, so
    probabilityMatchingLogNorm/selection group stats would fail to load from boxcar — you'd need those two refit under boxcar first. Want me to flag that anywhere, or is the 3-model set all you need?
